import 'package:flutter/material.dart';

class MyWikiScreen extends StatelessWidget {
  const MyWikiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('My Wiki Screen'),
      ),
    );
  }
}
